/**
 * GÉNÉRÉ AU BUILD — ce fichier source n'est qu'un gabarit de développement.
 * scripts/build.mjs remplace la version (empreinte du contenu) et la liste précachée
 * réelle du shell dans le fichier assemblé dist/sw.js.
 */
export const SW_VERSION = 'dev';
export const PRECACHE: readonly string[] = ['/index.html'];
